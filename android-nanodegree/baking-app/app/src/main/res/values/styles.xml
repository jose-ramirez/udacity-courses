<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeNoToolbar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- this affects only the toolbar text color, don't understand why yet -->
        <item name="android:textColorPrimary">#FFF</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <!--
        It's because of this that the video goes fullscreen when the phone is
        in landscape mode
    -->
    <style name="PlayerTheme" parent="android:Theme.Holo.NoActionBar.Fullscreen">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!--
        The cards should have more visible corners though
    -->
    <style name="BakingAppCardStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">4dp</item>
    </style>

    <!--
        The description that goes with the video of the recipe step is styled
        here; nothing fancy
    -->
    <style name="StepDescriptionStyle">
        <item name="android:textColor">#000</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">8dp</item>
        <item name="android:background">@color/cardview_light_background</item>
    </style>

    <!--
        How every item_fav_recipe, i.e., step, ingredient, recipe, will (or should) look
    -->
    <style name="BaseItemStyle">
        <item name="android:textColor">#000</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:background">@drawable/ripple</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingEnd">?android:attr/listPreferredItemPaddingEnd</item>
        <item name="android:textAppearance">?android:attr/textAppearanceListItemSmall</item>
        <item name="android:paddingStart">?android:attr/listPreferredItemPaddingStart</item>
    </style>

    <!--
        Here we set the shape and color for the fav button :)
    -->
    <style name="FavoriteButtonStyle" parent="Widget.AppCompat.Light.ActionButton">
        <item name="android:tint">@color/colorPrimary</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:src">@drawable/selector_favorite_button</item>
    </style>

    <!--
    Here we set the shape and color for the play button :)
-->
    <style name="PlayButtonStyle" parent="Widget.AppCompat.Light.ActionButton">
        <item name="android:tint">@color/colorPrimary</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:src">@drawable/selector_play_button</item>
    </style>

    <!--
         Here we set the player's height to 0, and let the weights do their
         job.
    -->
    <style name="PlayerViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
    </style>

    <!--
        The basic format for the error  messages when fetching the recipes'
        data.
    -->
    <style name="MessageViewStyle">
        <item name="android:textColor">#000</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center_horizontal|center_vertical</item>
        <item name="android:textAppearance">?android:attr/textAppearanceListItemSmall</item>
        <item name="android:paddingStart">?android:attr/listPreferredItemPaddingStart</item>
        <item name="android:paddingEnd">?android:attr/listPreferredItemPaddingEnd</item>
    </style>

</resources>
